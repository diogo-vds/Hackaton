spring:
  application:
    name: ms-vacinas
  datasource:
    url: ${DB_URL:jdbc:h2:mem:vacinas;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
server:
  port: ${SERVER_PORT:8080}
